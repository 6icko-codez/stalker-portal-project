# Stream URL Parsing - Before & After

## 🐛 The Bug (Before Fix)

```
┌─────────────────────────────────────────────────────────────┐
│ Stalker Portal Response                                     │
├─────────────────────────────────────────────────────────────┤
│ data.js.cmd = "ffmpeg http://portal.com/play/live.php?..."  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ OLD CODE: createLink() method                               │
├─────────────────────────────────────────────────────────────┤
│ const streamUrl = data.js.cmd.split(' ')[0];               │
│                                    ▲                         │
│                                    │                         │
│                          Takes first element only!          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ RESULT                                                       │
├─────────────────────────────────────────────────────────────┤
│ streamUrl = "ffmpeg"  ❌                                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Video Player                                                 │
├─────────────────────────────────────────────────────────────┤
│ Tries to load: /ffmpeg                                       │
│ Result: 404 Not Found ❌                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ The Fix (After)

```
┌─────────────────────────────────────────────────────────────┐
│ Stalker Portal Response                                     │
├─────────────────────────────────────────────────────────────┤
│ data.js.cmd = "ffmpeg http://portal.com/play/live.php?..."  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ NEW CODE: createLink() method                               │
├─────────────────────────────────────────────────────────────┤
│ Step 1: Trim whitespace                                     │
│   let streamUrl = data.js.cmd.trim();                       │
│                                                              │
│ Step 2: Remove "ffmpeg " prefix if present                  │
│   if (streamUrl.toLowerCase().startsWith('ffmpeg ')) {      │
│     streamUrl = streamUrl.substring(7).trim();              │
│   }                                                          │
│                                                              │
│ Step 3: Extract URL using regex (fallback)                  │
│   const urlMatch = streamUrl.match(/(https?:\/\/[^\s]+)/); │
│   if (urlMatch) streamUrl = urlMatch[1];                    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ RESULT                                                       │
├─────────────────────────────────────────────────────────────┤
│ streamUrl = "http://portal.com/play/live.php?mac=..."  ✅   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Video Player                                                 │
├─────────────────────────────────────────────────────────────┤
│ Loads: http://portal.com/play/live.php?mac=...              │
│ Result: Video plays successfully! ✅                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Code Comparison

### Before (Broken)

```typescript
async createLink(cmd: string, channelId: string): Promise<string | null> {
  try {
    const data = await this.makeRequest('itv', {
      action: 'create_link',
      cmd: encodeURIComponent(cmd),
      // ... other params
    });

    if (data.js?.cmd) {
      // ❌ WRONG: Only takes first word
      const streamUrl = data.js.cmd.split(' ')[0];
      return streamUrl;
    }

    return null;
  } catch (error) {
    console.error('Create link failed:', error);
    return null;
  }
}
```

**Input:** `"ffmpeg http://portal.com/play/live.php?mac=00:1A:79:XX:XX:XX"`  
**Output:** `"ffmpeg"` ❌

---

### After (Fixed)

```typescript
async createLink(cmd: string, channelId: string): Promise<string | null> {
  try {
    const data = await this.makeRequest('itv', {
      action: 'create_link',
      cmd: encodeURIComponent(cmd),
      // ... other params
    });

    if (data.js?.cmd) {
      // ✅ CORRECT: Extracts full URL
      let streamUrl = data.js.cmd.trim();
      
      // Remove "ffmpeg " prefix if present
      if (streamUrl.toLowerCase().startsWith('ffmpeg ')) {
        streamUrl = streamUrl.substring(7).trim();
      }
      
      // Regex fallback for edge cases
      const urlMatch = streamUrl.match(/(https?:\/\/[^\s]+)/);
      if (urlMatch) {
        streamUrl = urlMatch[1];
      }
      
      return streamUrl;
    }

    return null;
  } catch (error) {
    console.error('Create link failed:', error);
    return null;
  }
}
```

**Input:** `"ffmpeg http://portal.com/play/live.php?mac=00:1A:79:XX:XX:XX"`  
**Output:** `"http://portal.com/play/live.php?mac=00:1A:79:XX:XX:XX"` ✅

---

## 🧪 Test Cases

| Input | Old Output | New Output | Status |
|-------|-----------|-----------|--------|
| `"ffmpeg http://example.com/stream"` | `"ffmpeg"` ❌ | `"http://example.com/stream"` ✅ | Fixed |
| `"http://example.com/stream"` | `"http://example.com/stream"` ✅ | `"http://example.com/stream"` ✅ | Works |
| `"FFMPEG http://example.com/stream"` | `"FFMPEG"` ❌ | `"http://example.com/stream"` ✅ | Fixed |
| `"  ffmpeg   http://example.com/stream  "` | `""` ❌ | `"http://example.com/stream"` ✅ | Fixed |
| `"https://example.com/stream"` | `"https://example.com/stream"` ✅ | `"https://example.com/stream"` ✅ | Works |

---

## 🔄 Complete Request Flow

```
┌──────────────┐
│ Video Player │
└──────┬───────┘
       │ 1. Request stream
       ▼
┌─────────────────────────────────────────────┐
│ POST /api/iptv/stalker/stream               │
├─────────────────────────────────────────────┤
│ Body: {                                     │
│   portalUrl: "http://portal.com",           │
│   macAddress: "00:1A:79:XX:XX:XX",          │
│   cmd: "ffmpeg http://portal.com/play/...", │
│   channelId: "123"                          │
│ }                                            │
└──────┬──────────────────────────────────────┘
       │ 2. Initialize StalkerAPI
       ▼
┌─────────────────────────────────────────────┐
│ StalkerAPI.handshake()                      │
├─────────────────────────────────────────────┤
│ • Authenticates with portal                 │
│ • Receives token                            │
│ • Returns: true ✅                          │
└──────┬──────────────────────────────────────┘
       │ 3. Get subscription info (optional)
       ▼
┌─────────────────────────────────────────────┐
│ StalkerAPI.getSubscriptionInfo()            │
├─────────────────────────────────────────────┤
│ Returns: {                                  │
│   status: "active",                         │
│   expiryDate: "2025-12-31",                 │
│   daysRemaining: 60                         │
│ }                                            │
└──────┬──────────────────────────────────────┘
       │ 4. Create stream link
       ▼
┌─────────────────────────────────────────────┐
│ StalkerAPI.createLink(cmd, channelId)       │
├─────────────────────────────────────────────┤
│ Input: "ffmpeg http://portal.com/play/..."  │
│                                              │
│ Processing:                                  │
│ 1. Trim: "ffmpeg http://portal.com/play/..." │
│ 2. Remove prefix: "http://portal.com/play/..."│
│ 3. Regex match: "http://portal.com/play/..." │
│                                              │
│ Output: "http://portal.com/play/..." ✅      │
└──────┬──────────────────────────────────────┘
       │ 5. Return response
       ▼
┌─────────────────────────────────────────────┐
│ API Response                                 │
├─────────────────────────────────────────────┤
│ {                                            │
│   success: true,                             │
│   streamUrl: "http://portal.com/play/...",   │
│   metadata: {                                │
│     isM3U8: true,                            │
│     protocol: "http"                         │
│   },                                         │
│   subscription: {                            │
│     status: "active",                        │
│     daysRemaining: 60                        │
│   }                                          │
│ }                                            │
└──────┬──────────────────────────────────────┘
       │ 6. Load stream
       ▼
┌──────────────┐
│ Video Player │
├──────────────┤
│ Playing! ✅  │
└──────────────┘
```

---

## 📝 Summary

### What Was Wrong
- Used `.split(' ')[0]` which only took the first word
- Returned `"ffmpeg"` instead of the actual URL
- Video player received invalid URL and failed with 404

### What Was Fixed
1. **Step 1:** Trim whitespace from the response
2. **Step 2:** Check if string starts with "ffmpeg " and remove it
3. **Step 3:** Use regex to extract URL as fallback
4. **Result:** Always returns the full, valid stream URL

### Impact
- ✅ Video playback now works correctly
- ✅ Handles multiple URL formats
- ✅ Case-insensitive "ffmpeg" detection
- ✅ Robust regex fallback for edge cases
- ✅ Applied to all three methods: `createLink()`, `createVODLink()`, `createSeriesLink()`

---

**Date:** November 1, 2025  
**Status:** ✅ Fixed and Tested
